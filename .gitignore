# dependencies
node_modules

# secrets / env — NEVER commit
.env
.env.*
.env*.local
*.key
*.pem

# vercel
.vercel

# tooling / local
.claude
.playwright-mcp
.DS_Store

# raw source script PDF — copyrighted + large; the checked-in derived artifact is
# game/data/packs/hamilton.script.json (regenerate via scripts/extract-hamilton.py)
game/HAMILTON+Script.pdf

# player-generated translation-quality votes (local feedback sink, serve.mjs)
game/data/feedback.jsonl

# regenerable mockup screenshots (re-shoot from the .html)
design/mockups/*.png

# downloaded third-party reference images (copyrighted, scratch — not committed)
design/refs/
